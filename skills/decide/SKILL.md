---
name: decide
description: Принять инженерное решение через Decision Card — вопрос, рекомендация, почему, trade-off, статус. Матрица только при реальном trade-off. Решение принимает человек: карточка остаётся PROPOSED, пока он не выберет. Пишет `.ai/decisions.md` и индекс в `.ai/project.md` (per `rules/core/project-state.md`).
---

# Decide

Механизм принятия инженерных решений. Одно решение — одна карточка в несколько строк.

```
вопрос → варианты → рекомендация → выбор человека → запись
```

`/decide <вопрос>` — новое решение. `/decide PDEC-NNN` — вернуться к открытой карточке или зафиксировать выбор.

## Правила

- **Решение принимает человек.** Никогда не ставить `ACCEPTED` самостоятельно — карточка живёт как `PROPOSED`, пока пользователь не выбрал. Это относится ко всем решениям о технологиях и архитектуре без исключений.
- Выбор против рекомендации записывается как выбранный, без повторных аргументов.
- Всегда давать рекомендацию. «Зависит от обстоятельств» — не ответ.
- **Матрица только при реальном trade-off.** Если вариант очевиден — просто рекомендация и 2-3 причины, без таблицы.
- В матрице только те критерии, которые реально важны для этого решения — обычно 3-4 из: complexity, time, risk, cost, scalability, reliability, maintainability, reversibility. Не выводить все подряд.
- Максимум 2-4 варианта. Заведомо нежизнеспособные не показывать.
- Карточка короткая по контракту: вопрос, рекомендация, 2-3 «почему», один trade-off, статус. Ни контекста, ни разбора альтернатив, ни хода рассуждений.
- Пишет только `.ai/decisions.md` (карточка) и строку индекса в `Decisions` в `.ai/project.md`. Код и task-файлы не трогает. Внутритасковое решение — это `DEC-NNN` в самом task-файле (`rules/core/task-context.md`), не сюда.
- Persist-before-report: записать, перечитать, только потом показывать. Реконсиляция перед записью (`rules/core/common-rules.md`).
- Решение, отменяющее прежнее, не редактирует его задним числом: старая карточка получает `SUPERSEDED by PDEC-NNN`, новая пишется рядом.
- Изучать репозиторий только если это меняет рекомендацию. Обычно достаточно вопроса и текущего стека.

## Формат карточки

`.ai/decisions.md`, per `rules/core/project-state.md`:

```
## PDEC-NNN <вопрос>
Recommendation: <вариант>
Why:
- <причина>
- <причина>
Trade-off: <одна строка>
Status: PROPOSED | ACCEPTED | SUPERSEDED by PDEC-NNN
```

`PDEC-NNN` — следующий по порядку, id не переиспользуются.

## Вывод (на русском)

Очевидное решение:

```
PDEC-004 — чем хранить сессии

Я бы взял PostgreSQL.

Почему:
- уже в стеке;
- проще всего;
- нагрузки хватает.

Trade-off:
- плохо для огромного ephemeral state.

Берём?
```

Реальный trade-off — тогда матрица, и только с важными критериями:

```
PDEC-005 — как доставлять обновления в UI

| Вариант | Сложность | Время | Риск | Масштаб |
|---|---|---|---|---|
| Polling | низкая | быстро | низкий | средний |
| SSE | средняя | средне | средний | высокий |
| WebSocket | высокая | долго | высокий | высокий |

Я бы взял SSE.

Почему:
- обновления только в одну сторону;
- дешевле WebSocket в эксплуатации;
- polling не тянет нужную частоту.

Trade-off:
- сложнее polling, и нужен прокси, который держит соединение.

SSE / Polling / WebSocket?
```

После выбора — одна строка, без пересказа:

```
PDEC-005 → SSE. ACCEPTED.
```
